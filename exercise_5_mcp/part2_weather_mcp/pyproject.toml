[project]
name = "weather-mcp"
version = "0.1.0"
description = "MCP Server and Client for global weather data using Open-Meteo"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.2.0",
    "httpx>=0.27.0",
    "ollama>=0.4.0",
    "python-dotenv>=1.0.0",
]